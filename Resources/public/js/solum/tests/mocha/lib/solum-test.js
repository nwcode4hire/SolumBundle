module.exports = function(lib) {
  // Mocks to include
  var ko = require('../mocks/mock-ko.js');
  var $  = require('../mocks/mock-jquery.js');

  // Solum libraries to include
  var solum = require(lib + '/models/tables.js');

  describe('solum:tables:paginatedTable',function() {
    it('is pending');
  })
}